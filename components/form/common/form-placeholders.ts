export const imageUrlPlaceholder = (fileName: string) => (
  `https://exemple.com/${fileName}.png`
);

export const minecraftItemIdPlaceholder = (example: string) => (
  `Minecraft ID (ex : ${example})`
);
